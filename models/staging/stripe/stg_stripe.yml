version: 2

models:
  - name: stg_payments
    description: Staged payments data from stripe.
    columns: 
      - name: payment_id
        description: The primary key for payments.
        tests:
          - unique
          - not_null
